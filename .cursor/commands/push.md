# /push

Literally just pushes everything. Make sure you be aware that the `scrapers` directory in `src/arbihawk` is a submodule and thus, a separate repository. So push everything there first, then push everything in this repository, `myoro-arbihawk`
